services:
  web-dev:
    build:
      context: .
      dockerfile: Dockerfile # Menggunakan Node.js
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
